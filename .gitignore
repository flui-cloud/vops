node_modules
/lib
*.tsbuildinfo
.DS_Store
dev-tmp
.env
.env.local
.sonar/
.scannerwork/
Vops_ UI

# secrets / personal account files
ovh.openrc.sh
*.openrc.sh

# scratch plan artifacts
bm.json
vops-plan.json

# internal working notes
CLAUDE.md
.claude/

# docs/ is internal analysis + implementation plans, not public documentation.
# Keep docs/assets (logo + screenshot referenced by the public README) and
# docs/errors.md, which every structured error links to by URL.
docs/*
!docs/assets/
!docs/errors.md
# Local vops project state for this repo
.vops/
