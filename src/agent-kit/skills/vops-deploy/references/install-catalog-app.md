# Install a catalog application

List the catalog, describe the selected item, and generate its `flui.yaml` in the
repository. Show required variables, storage and public endpoints before planning.
The same validation, approval, deploy and verification gates as a custom
repository apply.
