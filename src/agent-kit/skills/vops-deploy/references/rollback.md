# Rollback

Rollback is available only when the capability registry reports it and a plan
describes a concrete strategy. Preserve data volumes by default. State when schema
migrations or external side effects make rollback conditional or impossible.
Never describe a redeploy as a guaranteed rollback.
