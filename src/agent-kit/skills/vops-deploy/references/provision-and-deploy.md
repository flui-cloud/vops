# Provision and deploy

Compare provider plans first and state the recurring price. A session must have a
non-zero spend limit covering the candidate. Server provisioning is high risk and
always requires explicit local approval.

After provisioning, inspect the resulting server, add or select the corresponding
target, then create a separate deployment plan. Approval for provisioning does not
approve deployment.
